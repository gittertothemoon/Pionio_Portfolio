export function Logo({ className }: { className?: string }) {
    return (
        <img
            src="/logo-wordmark.svg"
            alt="Pionio"
            className={className}
        />
    );
}
