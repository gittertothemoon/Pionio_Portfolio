# https://pionio.it/robots.txt
# Open to every crawler, AI search and training bots included: for a small studio, being known is worth more
# than the alternative. Only /api/ is off-limits. No per-bot groups on purpose: a group for one bot replaces
# this one for that bot, and would silently drop the /api/ rule.
User-agent: *
Allow: /
Disallow: /api/

Sitemap: https://pionio.it/sitemap.xml
